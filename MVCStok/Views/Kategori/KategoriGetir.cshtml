@model MVCStok.Models.Entity.KATEGORILER
@{
    ViewBag.Title = "KategoriGetir";
    Layout = "~/Views/Shared/_MainLayout.cshtml";
}

@using(Html.BeginForm("Güncelle", "Kategori", FormMethod.Post))
            {
                <div class="form-group">
                    @*@Html.LabelFor(x => x.KATEGORID)*@
                    @Html.HiddenFor(x => x.KATEGORID)
                    <br />
                    @Html.LabelFor(x => x.KATEGORIAD)
                    @Html.TextBoxFor(x => x.KATEGORIAD, new { @class = "form-control" })
                
                </div>
                <div>
                    <button class="btn btn-danger">Kategori Güncelle</button>
                </div>
            }
         

